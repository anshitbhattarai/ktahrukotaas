import Link from 'next/link'; import {games} from '@/lib/games';
export default function Games(){return <main className="wrap"><span className="tag">GAME LOBBY</span><h1>Choose a table</h1><p className="muted">Create or join a private room. Everyone must choose a username before sitting down.</p><div className="grid">{games.map(g=><Link href={`/table/${g.key}`} key={g.key} style={{textDecoration:'none',color:'inherit'}}><div className="card game"><div><div className="icon">{g.icon}</div><h2>{g.name}</h2><p className="muted">{g.desc}</p></div><button className="btn primary">Open lobby</button></div></Link>)}</div></main>}
